# mmonfar. // Semantic M&M Failure Navigation Engine
# Everything runs locally on CPU. No API keys required.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
sentence-transformers>=2.2
openpyxl>=3.1
pytest>=7.4

# Marketing / promo asset generation (Phase 4)
matplotlib>=3.7
imageio>=2.31

# Optional: better manifold separation than PCA.
# The engine auto-detects it and silently falls back to PCA if absent.
# umap-learn>=0.5
