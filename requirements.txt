# mmonfar. // Semantic M&M Failure Navigation Engine
# Everything runs locally on CPU. No API keys required.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
sentence-transformers>=2.2
openpyxl>=3.1
pytest>=7.4

# --smart-labels needs no extra install: `transformers` and `torch` already
# arrive with sentence-transformers. It downloads ~1GB of model weights on
# first use and runs offline thereafter.

# Optional: better manifold separation than PCA. The engine auto-detects it and
# falls back to PCA if absent.
# umap-learn>=0.5
